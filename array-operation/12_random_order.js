//数组乱序
function shuffle(arr) {
    let restTotal = arr.length;
    while(restTotal) {
        restTotal -= 1;
        let randomIndex = Math.floor(Math.random() * restTotal);
        let middleware = arr[index]
        arr[index] = arr[randomIndex]
        arr[random]
    }
}